import {
  TableRow,
} from "@mui/material";
import React from "react";

const TableDetailsMoto = ({ motorcycle }) => {
  console.log(motorcycle);
  return (
    <React.Fragment>
        <TableRow>

        </TableRow>
    </React.Fragment>
  );
};

export default TableDetailsMoto;
